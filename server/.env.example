# Server environment configuration

# Control server port (WebSocket for control plane)
CONTROL_PORT=8000

# Host to bind to (0.0.0.0 for all interfaces, or specific IP)
HOST=0.0.0.0

# Authentication tokens (comma-separated)
# Leave empty to disable authentication (not recommended for production)
AUTH_TOKENS=your-secret-token-here,another-token

# Allowed ports for tunnels
# Format: comma-separated ports or ranges (e.g., "3000,3001,5000-6000")
# Leave empty to allow all ports
ALLOWED_PORTS=3000-9000

# Connection timeout in milliseconds
CONNECTION_TIMEOUT=10000

# Ping interval in milliseconds (keepalive)
PING_INTERVAL=30000

# Ping timeout in milliseconds (disconnect if no response)
PING_TIMEOUT=60000

# Client data connection host (usually localhost or server's IP)
CLIENT_DATA_HOST=localhost

# Log level (trace, debug, info, warn, error, fatal)
LOG_LEVEL=info
